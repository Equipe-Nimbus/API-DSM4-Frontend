

name: Develop

on: 
  pull_request: 
    branches:
      - develop
    

jobs:

  TesteUnitario:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3
    
      - name: build container e aplicacao
        run: docker build -t teste .


  AceitaRecusaPR:
    runs-on: ubuntu-latest
    needs: TesteUnitario

    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        
      - name: Aprovando pull_request se os testes passarem
        id: aprovado
        uses: juliangruber/approve-pull-request-action@v2 
        with:
            github-token: ${{ secrets.ADM_TOKEN }}
            number: ${{ github.event.pull_request.number }}
  
      - name: Merge Pull Request
        if: steps.aprovado.outcome == 'success'
        uses: juliangruber/merge-pull-request-action@v1
        with:
          github-token: ${{ secrets.ADM_TOKEN }}
          number: ${{ github.event.pull_request.number }}
          method: squash
  
      - run: echo "HEAD:${{github.event.pull_request.head.repo.name}}, BASE:${{github.event.pull_request.base.repo.name}}"

  PushDokerHub:
    runs-on: ubuntu-latest

    needs: AceitaRecusaPR

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Configurar Docker para autenticação
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_TOKEN }}

      - name: Construir a imagem Docker
        run: |
          # Usando o número do pull request como parte da tag
          docker build -t pedrohsalmeida/nimbus-frontend:Dev-0.${{ github.event.number }} .
          docker tag pedrohsalmeida/nimbus-frontend:Dev-0.${{ github.event.number }} pedrohsalmeida/nimbus-frontend:Dev-latest

      - name: Push da imagem para o Docker Hub
        run: |
          docker push pedrohsalmeida/nimbus-frontend:Dev-0.${{ github.event.number }}
          docker push pedrohsalmeida/nimbus-frontend:Dev-latest



